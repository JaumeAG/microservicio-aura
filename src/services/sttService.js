async function transcribe(audioBase64) {
  // Aquí podrías usar Whisper API o Google Speech-to-Text
  // Por ejemplo (pseudo-código):
  //   const transcript = await whisperTranscribe(audioBase64);
  //   return transcript.text;
  return "Texto transcrito de ejemplo";  // placeholder
}

module.exports = { transcribe };
